<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/progress"
        >
        <rotate
            android:fromDegrees="0"
            android:pivotX="50%"
            android:pivotY="50%"
            android:toDegrees="360">
            <shape android:shape="ring">
                <size
                    android:width="10dp"
                    android:height="20dp" />
                <gradient
                    android:startColor="@color/chat_color_receiver"
                android:centerColor="@color/chat_color_sender"
                android:endColor="@color/chat_color_receiver"
                android:type="sweep" />
                <stroke
                    android:width="1dp"
                    android:color="@android:color/transparent" /> <!-- Set stroke color to transparent -->
            </shape>
        </rotate>
    </item>
</layer-list>
